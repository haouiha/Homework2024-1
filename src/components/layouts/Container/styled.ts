import styled from "styled-components";

export const ContainerStyled = styled.div(() => ({
  maxWidth: "1000px",
  display: "flex",
  justifyContent: "center",
  margin: "auto",
  alignItems: "center",
}));
